{
  "name": "@lvce-editor/wsl-monorepo",
  "version": "0.0.0-dev",
  "description": "WSL2 support for Lvce Editor",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node --experimental-strip-types packages/build/src/build.ts",
    "test": "npm test --workspaces --if-present",
    "e2e": "npm run e2e --workspace=@lvce-editor/wsl-e2e",
    "test:wsl": "npm run test:wsl --workspace=@lvce-editor/wsl-node",
    "type-check": "npm run type-check --workspace=@lvce-editor/wsl-node && npm run type-check --workspace=@lvce-editor/wsl-extension && npm run type-check --workspace=build && npm run type-check --workspace=@lvce-editor/wsl-e2e",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@lvce-editor/eslint-config": "^18.13.0",
    "eslint": "^10.9.1",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 150
  }
}
